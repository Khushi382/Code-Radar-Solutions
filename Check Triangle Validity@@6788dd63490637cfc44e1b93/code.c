#include <stdio.h>
int main() {
    int a,b,c;
    scanf("%d %d %d", &a , &b , &c);
    if(c^2=a^2+b^2){
        printf("Valid");
    }
    else{
        printf("Invalid");
    }
    
    return 0;
}